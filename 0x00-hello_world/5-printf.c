#include <stdio.h>
/**
 * main - C Program that prints exactly with print function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,/n");
	return (0);
}
